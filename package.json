{
    "name": "bloomreach-api-key-creator",
    "version": "1.0.0",
    "type": "module",
    "description": "Creates API keys in Bloomreach Engagement projects and returns project_token, secret_api_key, and api_key_id",
    "main": "src/main.js",
    "scripts": {
        "start": "node src/main.js"
    },
    "dependencies": {
        "apify": "^3.0.0",
        "playwright": "^1.40.0"
    }
}
